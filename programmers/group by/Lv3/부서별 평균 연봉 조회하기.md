```sql
-- Lv.3
-- 부서별 평균 연봉 조회하기

SELECT
    a.dept_id,
    b.dept_name_en,
    ROUND(a.avg_sal) AS avg_sal
FROM (
    SELECT
        dept_id,
        AVG(sal) AS avg_sal
    FROM hr_employees
    GROUP BY dept_id
) a
JOIN hr_department b ON a.dept_id = b.dept_id
ORDER BY avg_sal DESC;
```
```sql
-- Lv.3
-- 카테고리 별 도서 판매량 집계하기

SELECT
    category,
    SUM(sales) AS total_sales
FROM book a
JOIN book_sales b ON
    a.book_id = b.book_id AND
    b.sales_date BETWEEN '2022-01-01' AND '2022-01-31'
GROUP BY category
ORDER BY category;
```